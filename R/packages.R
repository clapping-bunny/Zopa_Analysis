#Load required packages
require(tidyr)
require(dplyr)
require(plyr)
require(ggplot2)
require(splines)
require(devtools)
require(tibble)
require(parallel)
require(readr)
require(hms)
require(ggplot2)

#require install via github
require(modelr)
require(purrr)
require(broom)